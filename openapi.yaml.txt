openapi: 3.1.0
info:
  title: YouTube Embeddable Video Search
  version: 1.0.0
  description: Securely search and retrieve embeddable YouTube videos via a Vercel proxy.
servers:
  - url: https://your-vercel-project.vercel.app
paths:
  /api/youtube:
    get:
      summary: Search YouTube videos via proxy and return only public, embeddable videos
      operationId: searchYouTube
      parameters:
        - name: q
          in: query
          required: true
          description: Search query term (e.g., "paris travel food")
          schema:
            type: string
        - name: maxResults
          in: query
          required: false
          description: Number of videos to return
          schema:
            type: integer
            default: 5
      responses:
        "200":
          description: Successful video search
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        snippet:
                          type: object
                          properties:
                            title:
                              type: string
                            description:
                              type: string
                            channelTitle:
                              type: string
                            thumbnails:
                              type: object
                              properties:
                                high:
                                  type: object
                                  properties:
                                    url:
                                      type: string
                        status:
                          type: object
                          properties:
                            embeddable:
                              type: boolean
                            privacyStatus:
                              type: string
                        player:
                          type: object
                          properties:
                            embedHtml:
                              type: string
